{
  "name": "hubot-corgi",
  "version": "0.3.0",
  "description": "Corgi pics and gifs from hubot!",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf lib/*",
    "build": "./node_modules/.bin/babel src --out-dir lib",
    "test": "./node_modules/.bin/mocha --compilers js:babel-core/register",
    "lint": "./node_modules/.bin/eslint src",
    "ci": "npm run lint && npm test"
  },
  "author": "Ryan Schultz",
  "license": "MIT",
  "repository": "git://github.com/ryands/hubot-corgi",
  "devDependencies": {
    "babel-cli": "^6.6.4",
    "babel-core": "^6.6.4",
    "babel-preset-es2015": "^6.6.0",
    "chai": "^3.5.0",
    "dotenv": "^2.x",
    "eslint": "^2.2.0",
    "gulp": "^3.9.1",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "lodash": "^4.x",
    "request": "^2.57.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
